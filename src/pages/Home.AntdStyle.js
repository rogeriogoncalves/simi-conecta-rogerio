import { primaryRed, primaryOrange, linkDefaultColor } from '../utils/colors';

const h1Style = { color: primaryRed, marginLeft: '17px' };

const innovationStyle = { backgroundColor: primaryOrange };

const linkStyle = { color: linkDefaultColor, textDecorationLine: 'underline' };

export { h1Style, innovationStyle, linkStyle };
