import { primaryRed } from '../utils/colors';

const h1Style = { color: primaryRed };

const listItemStyle = { margin: 0 };

const cardBodyStyle = { paddingTop: '10px', paddingBottom: '10px' };

export { h1Style, listItemStyle, cardBodyStyle };
