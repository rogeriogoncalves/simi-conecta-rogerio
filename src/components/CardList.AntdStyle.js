import { linkedinBlue } from '../utils/colors';

const linkedinIconStyle = { color: linkedinBlue };

const cardBodyStyle = { padding: '12px 15px' };

const infinityScrollStyle = { overflow: 'hidden' };

export { linkedinIconStyle, cardBodyStyle, infinityScrollStyle };
